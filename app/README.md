Run the project:

`nv run main.py`

Dependencies:

`"httpx>=0.28.1",
"mcp[cli]>=1.13.1",
"pytest>=8.4.1",
"pytest-asyncio>=1.1.0",
"pytest-mock>=3.14.1"`

1. HTTP streaming communication with the MCP server
2. In one time you can use one tool

Check in the Web Page by the:

`mcp inspector`

Flow:

1. Run mcp-server repo firstly 
2. After that by the script or UI you can create connection by the HTTP streaming
